<div class="container-new-ventes">
  <div class="row">
    <div class="col-xs-12 col-sm-10 col-sm-offset-1 col-md-4 col-md-offset-4">
      <%= simple_form_for(@vente) do |f| %>
      <%= f.input :name, placeholder: "Nom" %>
      <%= f.input :titre, placeholder: "titre" %>
      <%= f.input :description, placeholder: "Description" %>
      <%= f.input :price, placeholder: "Price" %>
      <%= f.input :surface, placeholder: "Surface" %>
      <%= f.button :submit, :value => 'Editer', class: "btn btn-primary" %>
      <% end %>
  </div>
  </div>
</div>
